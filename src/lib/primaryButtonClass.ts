/**
 * Solid primary (highlight) actions: white label and icons (use `currentColor` / `text-inherit` on icons).
 */
export const PRIMARY_BUTTON_CLASS =
  'bg-fg-highlight text-white font-sans hover:opacity-90 focus-visible:ring-2 focus-visible:ring-fg-highlight/50 focus-visible:outline-none'
